{
  "main": "src/index.ts",
  "scripts": {
    "build": "tsc --build",
    "clean": "tsc --build --clean",
    "start": "tsc --build && node src/index.js"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "ethers": "^5.5.2"
  },
  "dependencies": {
    "@debut/indicators": "^1.3.6",
    "dotenv": "^10.0.0"
  }
}
